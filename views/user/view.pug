extends ../layouts/common.pug
block content
   h1 View user
   a(href='/')
      button(class="btn btn-success ml-2") HOME
   a(href='/user')
      button(class="btn btn-success ml-2") BACK
   p
      b Name : 
      span= user.name
   b Age : 
   span= user.age
   